# Email delivery
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=username@example.com
SMTP_PASS=replace-me
SMTP_FROM="Mwein Medical <no-reply@mweinmedical.co.ke>"
CONTACT_EMAIL=appointments@mweinmedical.co.ke

# Admin dashboard access
ADMIN_SESSION_SECRET=replace-with-long-random-string

# Admin seeding helper (optional; only read by `npm run seed:admin`)
# ADMIN_SEED_EMAIL=admin@example.com
# ADMIN_SEED_PASSWORD=super-secure-password
# ADMIN_SEED_ROLE=ADMIN

# Database configuration
# Local default keeps SQLite for convenience. Update DATABASE_PROVIDER when
# switching to Postgres (e.g. on Vercel) so Prisma targets the correct engine.
DATABASE_URL="file:./prisma/dev.db"
DATABASE_PROVIDER=sqlite

# Production/PostgreSQL example (remember to change DATABASE_PROVIDER)
# DATABASE_URL="postgresql://user:password@host:5432/mwein_medical?schema=public"
# DATABASE_PROVIDER=postgresql

# Site metadata
NEXT_PUBLIC_SITE_URL=https://mweinmed.com
# SITE_URL=https://mweinmed.com
